import { FilterGiftsPipePipe } from './filter-gifts-pipe-pipe';

describe('FilterGiftsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGiftsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
